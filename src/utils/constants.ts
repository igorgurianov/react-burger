type TingridientTypes = {
  bun: string;
  main: string;
  sauce: string;
};

const ingridientTypes: TingridientTypes = {
  bun: "bun",
  main: "main",
  sauce: "sauce",
};

export default ingridientTypes;
